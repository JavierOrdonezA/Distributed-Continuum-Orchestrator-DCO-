# Puerto donde se ejecutará la aplicación dentro del contenedor
HOST_PORT=8080

# Ubicación de instalación dentro del contenedor
INSTALL_LOC=/app

# Valores predeterminados para las variables que se pueden sobrescribir en docker-compose.yml
STATE_MACHINE_TYPE=default
NODE_NAME=default
